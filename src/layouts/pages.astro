---
import BaseLayout from './base.astro'
import ArticleComponent from '@components/article.astro'

const { article } = Astro.props
---
<BaseLayout title={article.title}>
    <ArticleComponent article={article} isInPage={true}>
        <slot/>
    </ArticleComponent>
</BaseLayout>